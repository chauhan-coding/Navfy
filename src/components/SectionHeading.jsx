/**
 * Re-export from common/ to preserve backward compatibility with existing section imports.
 * Prefer importing from 'components/common/SectionHeading' in new code.
 */
export { default } from './common/SectionHeading'
